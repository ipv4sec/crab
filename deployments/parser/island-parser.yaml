---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: island-parser-user
  namespace: island-system
---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: island-parser-user-building
  namespace: island-system
subjects:
  - kind: ServiceAccount
    name: island-parser-user
    apiGroup: ""
    namespace: island-system
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: island-parser
  namespace: island-system
  labels:
    app: island
    component: parser
spec:
  replicas: 1
  template:
    metadata:
      name: island-parser
      labels:
        app: island
        component: parser
    spec:
      containers:
        - name: island-parser
          image: harbor1.zlibs.com/island/island-parser:alpha
          imagePullPolicy: Always
      restartPolicy: Always
      serviceAccountName: island-parser-user
  selector:
    matchLabels:
      app: island
      component: parser
---
apiVersion: v1
kind: Service
metadata:
  name: island-parser
spec:
  selector:
    app: island
    component: parser
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
---