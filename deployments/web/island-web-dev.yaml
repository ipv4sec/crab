apiVersion: v1
kind: Service
metadata:
  name: island-web-dev
spec:
  selector:
    app: island
    component: web
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30600
---